services:
  web:
    build: .
    restart: unless-stopped
    ports:
      - "80"
    volumes:
      - wind-data:/data
    networks:
      - dokploy-network

volumes:
  wind-data:

networks:
  dokploy-network:
    external: true
