services:
  web:
    build: .
    restart: unless-stopped
    ports:
      - "3000"
    volumes:
      - wind-data:/data
    networks:
      - dokploy-network

volumes:
  wind-data:

networks:
  dokploy-network:
    external: true
